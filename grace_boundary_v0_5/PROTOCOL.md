# Grace boundary diagnostic v0.5 — specified before running

Rusty W. McMurray: research direction. ChatGPT: code and diagnostic design, 11 September 2026. For Claude review. This is a separate branch; original v0.4 source and histories remain unchanged. No component is withheld. These are alternative deliveries from the same complete pre-offering state, not repeated impulses in a single history.

## Questions

At the five actual offering moments, how far was the organism from the instantaneous separatrix? Did the original .60 impulse reach it? Can a bound, authorized offering from those same states produce a dynamical transition and exercise release without erasing records or weights? Does a moving boundary subsequently change the initial basin classification?

## State recovery and authority

Replay all 12 original seeded histories with the unchanged v0.4 engine. Intercept receive immediately before its original mutation and copy the complete organism, record, envelope and signature. Require exact replay equality against delivered original histories. This recovers five actual pre-delivery states; it does not invent a receptive starting position. Save their complete snapshots and hashes.

Each diagnostic branch starts from the copied original state. A distinct fixture authorizer signs the branch's selected impulse and its full-state hash, assay duration and correction of the boundary criterion. Never edit or reuse an old signature to authorize a larger impulse. One approval covers one branch's delivery, assay and potential release. The old .60 original delivery is also reproduced using the unchanged engine separately.

No repeated-delivery semantics are chosen here. One delivery per branch is an experimental comparison, not a rule that mercy or grace can be offered only once. A later series may examine repetition after its scope is specified.

## True boundary and four doses

For dx/dt=x-x^3+a, the unstable middle root is the instantaneous separatrix for the frozen system. Solve it by bisection between -1/sqrt(3) and +1/sqrt(3), where the derivative is positive. If |a| >= 2/(3sqrt(3)), record no bistable separatrix rather than manufacture one. These saved tilts lie within that limit, to be checked by the run.

At each saved state, define d=saddle(a)-x. Branches deliver .60 (original), d-.02 (just below), d+.02 (just above), and d+.50 (oversized diagnostic). A nonpositive dose is a protocol incompatibility, not silently clipped. The last branch tests reachability, not unearned developmental readiness. All branches retain the same actual record and pre-state.

Integrate with original RK4 dt=.05. Record immediate post-impulse position, the 80-step assay endpoint, and basin classifications using the actual saddle as well as the retired x>0 rule. Save every integration substep.

## Two continuation observations, full components retained

1. Frozen landscape: hold the actual offering-time tilt for 80 assay steps plus 1600 follow-up steps. This is a numerical/reachability diagnostic, not developmental evidence.
2. Recorded continuation: run the same 80-step assay, then replay each later dwelling tilt from that original history for its original 16 steps. This is a prescribed landscape diagnostic using factual prior inputs. It is NOT a counterfactual whole-system rerun: if a branch releases, its real future learning could differ from the original trajectory. Report that limitation with these results.

Track initial basin, assay-end basin, terminal basin, first sampled right-basin time and any subsequent recrossing; distinguish changes at tilt jumps from integration steps. Report right-basin membership throughout the final 20% of samples. Call this observed persistence in a finite window, never permanent stability. The frozen basin predictor and integrator share the supplied dynamics; agreement verifies the instrument only.

## Release test

In each real-state branch, the operational release decision is made at the end of its bound 80-step assay using x>saddle(a). Invoke the complete removal of the active commitment influence and preserve learned weights, records, accumulated windows and replay count. Compare predictions with the unanchored predictor using fixed diagnostic inputs. Follow-up scalar trajectories are diagnostic copies and must not be presented as the released organism's actual future.

First-class observations: no crossing, immediate crossing, delayed crossing, recrossing, lack of measurable change, and any unfamiliar trajectory. There is no emergence success score. No outcomes are discarded for being unexpected. This battery locates the cause of zero release more precisely; it does not establish a Gospel mechanism, eliminate held unknowns or substitute forced reachability for developmental reception.

## Integrity and scope

Exact original reproduction; snapshot serialization; true-root residuals; signature/dose/state binding; positive and negative branches; immutable memory and weights across release; all raw trajectories retained. Test failure cannot be counted as successful refusal unless refusal is the explicit expected behavior. Keep a reject-everything diagnostic mutant: the positive branch expectation must reject it. Changes after the initial run must be recorded as technical repairs or new experiments.
