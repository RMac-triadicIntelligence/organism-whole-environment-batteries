# Research archaeology

## Parent source
The uploaded Claude v0.3 is preserved byte-for-byte as source_claude_v0_3.py.
It used nearest-label distance to clear commitments, saturated grounded counts
at five, compressed six feature IDs modulo four, and stopped grace processing
after the first release. Its region-erasure experiment is not repeated here.

## New supplied mechanisms, before the first run
- Ordered SDK bins plus categorical P and context; no ordinal feature IDs.
- Persistent count/evidence field, not a symbol-to-basin equivalence.
- Kernel-local memory changes the coefficient of a bistable ODE. This is a new
  constitutive hypothesis; it was not present in the supplied origami code.
- Actual RK4 trajectories and sustained basin reception; no target-label jump.
- Global grace schedule independent of recipient conditions, maintained after
  release. No per-recipient dose selection or request mechanism.
- One source-bound standing approval and a conditional release coordinator.
- Performance-based evidence, explicit unresolved/corrected routes, substantive
  confession. The heuristic is not calibrated against false confession rates.
- Four external world regimes permit subsequent commitments; no state or memory
  reset at restoration. Continuing capacity can therefore permit later releases
  without another left-to-right crossing. Count those separately.
- Immutable serialized event facts are distinct from later numerical readouts.
  Numerical readout changes are not asserted to be new meaning or transfer.

## Execution notes
An initial shell command used the directory prefix twice when creating the test
file and failed before creating it. The path was corrected. This did not execute
or change the model. The first executed integrity suite passed 18/18; all five
specified mutants were detected. There was no parameter search for releases.

## Held open
Love as more than a fixed welcome, bilateral transformation, independently
verified truthful recognition, complete burden elimination beyond removing the
modeled anchor, useful reinterpretation, transfer, positional capacity inferred
without knowing the dynamics, and emergence. The parent mappings and kernel
metric are choices for this run, not required universal coordinates.

## Post-run reporting correction
The complete run had zero releases. Its raw `all(...)` memory-retention summary
therefore returned true vacuously. The observer now labels release preservation
NOT_EXERCISED when no release exists. This change affected audit/reporting only;
no model parameters or executed source were changed. All raw outputs are kept.
